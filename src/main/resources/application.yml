spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: user
    password: 1234
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create
server:
  servlet:
    context-path: /app
